#include "main.h"

/**
 * add - This function adds two integers and returns the result
 * @sum: number to be checked
 * Return: 0
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
